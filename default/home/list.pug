extends ../../views/list.pug
block info
block content    
block facetBanner
    .wlp
        script(src="/home/static/d3.v3.min.js")
        script(src="/home/static/chart.js")
        input#chartS.hid(type="radio" name="chsz" checked=true)
        input#chartZ.hid(type="radio" name="chsz")
        div.btg.right.rnd.pad
            label.out.pad.bor.vgi-table(for="chartS",style="text-align:right;") Grid
            label.out.pad.bor.vgi-box(for="chartZ",style="text-align:right;") Large
        #charts.grd.pad
        script.
            // Escape < so a value containing a closing script tag cannot end this block.
            var charts = !{JSON.stringify(docs).replace(/</g, '\\u003c')};
            plotCharts("charts", charts);